{
  "name": "feature-harness",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @harness/api dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "tsx": "^4.22.4",
    "typescript": "^5.8.3"
  }
}
